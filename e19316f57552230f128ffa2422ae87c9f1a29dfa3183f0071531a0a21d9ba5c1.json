{
  "verdict": "TRUE_POSITIVE",
  "comment_markdown": "## Triage Result\n\n**Decision:** TRUE POSITIVE\n**Category:** N/A\n**Confidence:** High\n**Vulnerability Class:** XSS (Cross-Site Scripting / Server-Side Template Injection)\n\n**Summary:** The user-controlled username stored in the database is inserted unsanitized into the raw Pug template source at `routes/userProfile.ts:74`; the backslash prefix added at `routes/userProfile.ts:67` only escapes Pug interpolation syntax and does not prevent HTML injection, and the `eval(code)` branch at `routes/userProfile.ts:62` executes arbitrary expressions in the Node.js server process.\n\n### Code\n\n`routes/userProfile.ts:53-74`\n\n```typescript\nlet username = user.username\n\nif (username?.match(/#{(.*)}/) !== null && utils.isChallengeEnabled(challenges.usernameXssChallenge)) {\n  req.app.locals.abused_ssti_bug = true\n  const code = username?.substring(2, username.length - 1)\n  try {\n    if (!code) { throw new Error('Username is null') }\n    username = eval(code) // eslint-disable-line no-eval\n  } catch (err) {\n    username = '\\\\' + username\n  }\n} else {\n  username = '\\\\' + username\n}\n// ...\nif (username) {\n  template = template.replace(/_username_/g, username)\n}\n```\n\nThe username from the database is conditionally executed via `eval()` or prefixed with a backslash, then inserted verbatim into the raw Pug template source string before compilation and rendering.\n\n### Review Scope\n\n**Files examined:** `routes/userProfile.ts`, `views/userProfile.pug`, `lib/insecurity.ts`\n**Searched for:** `authenticatedUsers` in `lib/insecurity.ts`; `_username_` placement and rendering context in `views/userProfile.pug`\n\n### Stage 1 — Vulnerability Verification\n\n**Vulnerability in Code:** TRUE\n\nAt `routes/userProfile.ts:62`, `username = eval(code)` executes the extracted expression from a `#{...}`-patterned username in the live Node.js process with no sandboxing. At `routes/userProfile.ts:74`, the modified username is substituted directly into the raw Pug template source via `template.replace(/_username_/g, username)`. The placeholder `_username_` at `views/userProfile.pug:44` appears as inline literal text inside a `<p>` tag (`p(style='...') _username_`); Pug does not auto-escape literal template text (escaping applies only to `=` and `#{}` variable bindings), so injected HTML tags in the username are emitted verbatim in the rendered response. The SAST code flow from `routes/userProfile.ts:35` (cookie) through `routes/userProfile.ts:42` (DB lookup) to `routes/userProfile.ts:98` (send) correctly identifies a real data-flow path to an unsanitized HTML sink.\n\n### Stage 2 — Impact Assessment\n\n**Impact:** TRUE\n\nThe `eval(code)` branch at `routes/userProfile.ts:62` grants arbitrary code execution in the Node.js server process, not merely in the browser, making this higher-severity than a typical reflected XSS. For usernames not matching the `#{...}` pattern, injecting HTML tags (e.g., `<script>`, `<img onerror=...>`) into the template source at `routes/userProfile.ts:74` yields JavaScript execution in the victim's browser, enabling session hijacking, credential theft, and account takeover. Per the production code assumption, both attack paths pose direct threats to user data integrity and server security.\n\n### Stage 3 — Reachability Assessment\n\n**Exploitable:** TRUE\n\n- **External Input:** TRUE — Usernames are set by authenticated users via registration and profile-update APIs; the session cookie at `routes/userProfile.ts:35` is fully user-controlled and selects whose stored data is rendered.\n- **Trust Level:** Untrusted — Both the cookie and the database-stored username originate from user-supplied input and are not validated at deployment time.\n- **Taint Flow:** Viable — Username flows from `UserModel.findByPk(loggedInUser.data.id)` at `routes/userProfile.ts:42` through the conditional processing at `routes/userProfile.ts:55–68` (the backslash prefix prevents Pug SSTI but does not HTML-encode the value) and is inserted as raw text into the Pug template source at `routes/userProfile.ts:74` with no intervening HTML sanitization.\n- **Sink Usage:** Dangerous — `pug.compile(template)` at `routes/userProfile.ts:87` compiles a template that may contain attacker-injected HTML or executable Pug expressions; `res.send(fn(user))` at `routes/userProfile.ts:98` delivers the rendered HTML to the client.\n\n### Primary FP Reason\n\nN/A\n\n### Additional FP Reasons\n\nNone.\n\n### Compensating Controls Found\n\n- The backslash prefix at `routes/userProfile.ts:67` (`username = '\\\\' + username`) neutralises Pug interpolation syntax `#{...}` and prevents SSTI via Pug's own template engine, but it does not HTML-encode special characters and therefore does not prevent injection of raw HTML tags (e.g., `<script>`, `<img onerror=...>`).\n- The Content Security Policy constructed at `routes/userProfile.ts:88` (`script-src 'self' 'unsafe-eval'`) blocks inline `<script>` blocks that lack a nonce, but explicitly includes `'unsafe-eval'`, leaving the `eval()`-based SSTI attack vector fully open; inline event handlers (`onerror`, `onclick`, etc.) are also not restricted by this policy.\n- The endpoint requires a valid authenticated session (`routes/userProfile.ts:36–38`), confining exploitation to authenticated users operating on their own profile (self-XSS); however, if an administrator or another privileged user can view a target's profile, the stored payload escalates to a cross-user stored XSS against a privileged account.\n\n### Key Facts\n\n- `routes/userProfile.ts:35`: `req.cookies.token` is used as a lookup key in `security.authenticatedUsers.get()`; the returned session object's `.data.id` (integer) drives the subsequent database query.\n- `routes/userProfile.ts:42`: `UserModel.findByPk(loggedInUser.data.id)` returns the full database user record including the user-controlled `username` field.\n- `routes/userProfile.ts:62`: `username = eval(code)` evaluates the body of a `#{...}`-patterned username in the Node.js server process with no sandbox or allow-list, constituting server-side code execution.\n- `routes/userProfile.ts:67` and `routes/userProfile.ts:74`: The backslash prefix escapes Pug SSTI but the username is then inserted as raw literal text into the Pug source; Pug does not encode literal text, only variable-binding outputs.\n- `views/userProfile.pug:44`: `p(style='...') _username_` — the placeholder is inline literal text in a `<p>` block, a context where Pug emits the substituted value without HTML encoding.\n- `routes/userProfile.ts:88`: CSP header includes `'unsafe-eval'`, which permits `eval()` and `Function()` calls and directly enables the SSTI exploitation path in the browser as well as the server.\n- `routes/userProfile.ts:90–92`: The `usernameXssChallenge` solve-condition checks `utils.contains(username, '<script>alert(\\`xss\\`)</script>')`, confirming the code path is designed to pass `<script>` tags through to the rendered HTML output."
}
